This is a simple example how to use variadic templates with type traits.
N.B.: this won't compile with MSVS(it will generate an error) because of the fold expression,
but I've sent a message to Microsoft to fix that bug.
